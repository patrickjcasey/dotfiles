# this script puts config and data files in the correct spot for an offline install
tar zxvf dotfiles.tar.gz
cp -r dotfiles/.local ~
cp -r dotfiles/.config ~
cp dotfiles/.zshrc ~
cp dotfiles/.tmux.conf ~/.tmux.conf
cp -r dotfiles/.tmux ~/.tmux
